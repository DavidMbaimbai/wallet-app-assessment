spring:
  mail:
    host: smtp.gmail.com
    port:  465
    username: davidm.mbaimbai@gmail.com
    password: afckpftcafblmzwy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "3.0"

accounts:
  message: "Welcome to EazyBank accounts related local APIs"
  contactDetails:
    name: "David Mbaimbai - Java Developer"
    email: "davymbaimbai@gmail.com"
  onCallSupport:
    - (555) 555-1234
    - (555) 523-1345
